from core.point import Point
from core.figure import Figure

class ShapeDetector:
    """
    Detects possible geometric figures from a set of points.
    """
    @staticmethod
    def detect_shapes(point_list: list[Point]) -> list[Figure]:
        pass

    @staticmethod
    def combine_three_points(points: list[Point]) -> list[list[Point]]:
        pass

    @staticmethod
    def combine_four_points(points: list[Point]) -> list[list[Point]]:
        pass