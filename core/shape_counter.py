from core.figure import Figure

class ShapeCounter:
    """
    Counts how many figures exist of each type.
    """
    @staticmethod
    def count_by_type(figures: list[Figure]) -> dict[str, int]:
        pass